// index.js
import React from 'react';
import ReactDOM from 'react-dom';
import App from './App.jsx'; // Ensure the path and filename match your project structure

ReactDOM.render(<App />, document.getElementById('root'));
